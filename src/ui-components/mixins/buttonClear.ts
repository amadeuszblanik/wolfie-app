import { css } from "styled-components";

const mixin = () => {
  return css`
    background: transparent;
    border: none;
    border-radius: 0;
    appearance: none;
  `;
};

export default mixin;
